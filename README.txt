This script scans for email addresses or phone numbers in your text.

Dump text into data.txt or rename your text file to data.txt(making sure its in the same directory as finder.py) before running the program.